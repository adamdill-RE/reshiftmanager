# Local, machine-specific configuration. Contains credentials — never committed.
/config/config.local.php

# Runtime state written by the app (sessions, caches, logs). Lives outside
# public/ and is recreated on demand.
/var/

# Editor and OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Composer is deliberately unused (see CLAUDE.md); guard against a stray install
/vendor/
composer.lock
