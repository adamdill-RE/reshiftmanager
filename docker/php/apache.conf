# .htaccess has to be honoured for the front controller rewrite to work; the
# base Debian image sets AllowOverride None.
<Directory /var/www/html>
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
